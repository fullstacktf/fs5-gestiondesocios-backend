name: API tests

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v2

            - name: Run docker compose
              run: |
                  docker-compose up -d

            - name: Exec docker GO and tests
              run: |
                  docker exec -t app_go sh -c 'cd api/tests && go test -v'

            - name: Stops docker containers
              run: |
                  chmod +x ./stopdockers.sh && ./stopdockers.sh
